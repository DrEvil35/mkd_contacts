const fallbackData = {
  "title": "Контакты по подъездам",
  "description": "Выберите свой подъезд, чтобы увидеть ответственных и удобные способы связи.",
  "footer": "Для связи нажмите на нужный подъезд, затем выберите человека и соцсеть.",
  "people": [
    {
      "id": "chairperson",
      "fullName": "Председатель совета дома",
      "role": "Председатель",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/i_kill_you_nax",
          "label": "Написать в VK"
        },
        {
          "type": "telegram",
          "url": "https://t.me/Haskell35",
          "label": "Написать в Telegram"
        },
        {
          "type": "max",
          "url": "https://max.ru/u/f9LHodD0cOL2_iK3o2berTpWfjcf1UFkm0GFnBEvH4FOQdmdR0J31PPRXEg",
          "label": "Написать в MAX"
        }
      ]
    },
    {
      "id": "vk-id88635166",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/id88635166",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-dobriakova_annetta_astro",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/dobriakova_annetta_astro",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-id35704209",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/id35704209",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-id135852948",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/id135852948",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-svetlashshka",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/svetlashshka",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-id133296016",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/id133296016",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-id145319957",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/id145319957",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-ananac1k",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/ananac1k",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-marushashalfey",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/marushashalfey",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-id434704139",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/id434704139",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-id68547",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/id68547",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "vk-id9854585",
      "fullName": "ФИО не указано",
      "role": "Ответственный по подъезду",
      "contacts": [
        {
          "type": "vk",
          "url": "https://vk.com/id9854585",
          "label": "Написать в VK"
        }
      ]
    },
    {
      "id": "reserve-person",
      "fullName": "Замещающее лицо",
      "role": "Замещающий контакт",
      "contacts": []
    }
  ],
  "entrances": [
    {
      "label": "1 подъезд",
      "personIds": [
        "chairperson",
        "vk-id88635166",
        "reserve-person"
      ]
    },
    {
      "label": "2 подъезд",
      "personIds": [
        "chairperson",
        "vk-dobriakova_annetta_astro",
        "reserve-person"
      ]
    },
    {
      "label": "3 подъезд",
      "personIds": [
        "chairperson",
        "vk-id35704209",
        "reserve-person"
      ]
    },
    {
      "label": "4 подъезд",
      "personIds": [
        "chairperson",
        "vk-id135852948",
        "reserve-person"
      ]
    },
    {
      "label": "5 подъезд",
      "personIds": [
        "chairperson",
        "vk-dobriakova_annetta_astro",
        "reserve-person"
      ]
    },
    {
      "label": "6 подъезд",
      "personIds": [
        "chairperson",
        "vk-dobriakova_annetta_astro",
        "reserve-person"
      ]
    },
    {
      "label": "7 подъезд",
      "personIds": [
        "chairperson",
        "vk-svetlashshka",
        "reserve-person"
      ]
    },
    {
      "label": "8 подъезд",
      "personIds": [
        "chairperson",
        "vk-id133296016",
        "reserve-person"
      ]
    },
    {
      "label": "9 подъезд",
      "personIds": [
        "chairperson",
        "vk-id145319957",
        "reserve-person"
      ]
    },
    {
      "label": "10 подъезд",
      "personIds": [
        "chairperson",
        "vk-ananac1k",
        "reserve-person"
      ]
    },
    {
      "label": "11 подъезд",
      "personIds": [
        "chairperson",
        "vk-marushashalfey",
        "reserve-person"
      ]
    },
    {
      "label": "12 подъезд",
      "personIds": [
        "chairperson",
        "vk-id434704139",
        "reserve-person"
      ]
    },
    {
      "label": "13 подъезд",
      "personIds": [
        "chairperson",
        "vk-id68547",
        "reserve-person"
      ]
    },
    {
      "label": "14 подъезд",
      "personIds": [
        "chairperson",
        "vk-id9854585",
        "reserve-person"
      ]
    }
  ]
};

let pageData = null;
let peopleById = {};

const entranceList = document.getElementById('entrance-list');
const modal = document.getElementById('contact-modal');
const modalTitle = document.getElementById('modal-title');
const modalSubtitle = document.getElementById('modal-subtitle');
const modalContacts = document.getElementById('modal-contacts');
const debugError = document.getElementById('debug-error');

const socialMeta = {
  vk: {
    name: 'VK',
    icon: 'VK'
  },
  telegram: {
    name: 'Telegram',
    icon: 'TG'
  },
  max: {
    name: 'MAX',
    icon: 'MAX'
  },
  phone: {
    name: 'Телефон',
    icon: '☎'
  },
  email: {
    name: 'Email',
    icon: '@'
  },
  whatsapp: {
    name: 'WhatsApp',
    icon: 'WA'
  }
};

function createElement(tag, className, text) {
  const element = document.createElement(tag);

  if (className) {
    element.className = className;
  }

  if (text !== undefined && text !== null) {
    element.textContent = text;
  }

  return element;
}

function normalizeType(type) {
  return String(type || 'link').trim().toLowerCase();
}

function getInitials(fullName) {
  const cleanName = String(fullName || '').trim();

  if (!cleanName || cleanName === 'ФИО не указано') {
    return '?';
  }

  return cleanName
    .split(/\s+/)
    .filter(Boolean)
    .slice(0, 2)
    .map(function (word) {
      return word.charAt(0).toUpperCase();
    })
    .join('');
}

function createContactLink(link) {
  const type = normalizeType(link.type);
  const meta = socialMeta[type] || { name: link.type || 'Ссылка', icon: '↗' };

  const a = document.createElement('a');
  a.className = 'contact-link contact-link-' + type;
  a.href = link.url;
  a.target = '_blank';
  a.rel = 'noopener noreferrer';

  const logo = createElement('span', 'contact-logo', meta.icon);
  const textWrap = createElement('span', 'contact-link-text');
  const label = createElement('span', 'contact-link-label', link.label || meta.name);
  const hint = createElement('span', 'contact-link-hint', meta.name);

  textWrap.appendChild(label);
  textWrap.appendChild(hint);

  a.appendChild(logo);
  a.appendChild(textWrap);

  return a;
}

function createPersonCard(person) {
  const card = createElement('article', 'person-card');

  const header = createElement('div', 'person-header');
  const avatar = createElement('div', 'person-avatar', getInitials(person.fullName));
  const text = createElement('div', 'person-text');
  const role = createElement('div', 'person-role', person.role || 'Контакт');
  const name = createElement('div', 'person-name', person.fullName || 'Без имени');

  text.appendChild(role);
  text.appendChild(name);
  header.appendChild(avatar);
  header.appendChild(text);
  card.appendChild(header);

  const linksWrap = createElement('div', 'contact-links');

  if (Array.isArray(person.contacts) && person.contacts.length) {
    person.contacts.forEach(function (link) {
      if (link && link.url) {
        linksWrap.appendChild(createContactLink(link));
      }
    });
  } else {
    linksWrap.appendChild(createElement('div', 'empty-links', 'Контакты будут добавлены позже'));
  }

  card.appendChild(linksWrap);
  return card;
}

function openModal(entrance) {
  modalTitle.textContent = entrance.label;
  modalSubtitle.textContent = 'Выберите человека и подходящий канал связи.';

  modalContacts.innerHTML = '';

  const personIds = Array.isArray(entrance.personIds) ? entrance.personIds : [];

  personIds.forEach(function (personId) {
    const person = peopleById[personId];

    if (person) {
      modalContacts.appendChild(createPersonCard(person));
    } else {
      modalContacts.appendChild(createElement('div', 'error', 'Контакт с id «' + personId + '» не найден в people.'));
    }
  });

  modal.hidden = false;
  document.body.classList.add('modal-open');

  const closeButton = modal.querySelector('.modal-close');
  closeButton.focus();
}

function closeModal() {
  modal.hidden = true;
  document.body.classList.remove('modal-open');
}

function validateData(data) {
  if (!data || typeof data !== 'object') {
    throw new Error('contacts.json должен содержать объект.');
  }

  if (!Array.isArray(data.people)) {
    throw new Error('В contacts.json нет массива people.');
  }

  if (!Array.isArray(data.entrances)) {
    throw new Error('В contacts.json нет массива entrances.');
  }
}

function renderPage(data) {
  validateData(data);

  document.title = data.title || document.title;
  document.getElementById('page-title').textContent = data.title || 'Контакты по подъездам';
  document.getElementById('description').textContent = data.description || '';
  document.getElementById('footer').textContent = data.footer || '';

  peopleById = {};
  data.people.forEach(function (person) {
    if (person && person.id) {
      peopleById[person.id] = person;
    }
  });

  entranceList.innerHTML = '';

  data.entrances.forEach(function (entrance) {
    const button = document.createElement('button');
    button.className = 'entrance-button';
    button.type = 'button';

    const number = String(entrance.label || '').replace(/[^0-9]/g, '') || '?';

    const numberElement = createElement('span', 'entrance-number', number);
    const textElement = createElement('span', 'entrance-text', 'подъезд');
    const arrowElement = createElement('span', 'entrance-arrow', '›');

    button.appendChild(numberElement);
    button.appendChild(textElement);
    button.appendChild(arrowElement);

    button.addEventListener('click', function () {
      openModal(entrance);
    });

    entranceList.appendChild(button);
  });
}

async function loadContacts() {
  try {
    const response = await fetch('contacts.json', { cache: 'no-store' });

    if (!response.ok) {
      throw new Error('contacts.json не загружен: HTTP ' + response.status);
    }

    pageData = await response.json();
    renderPage(pageData);
  } catch (error) {
    console.warn('Используется встроенная резервная копия данных.', error);
    debugError.hidden = false;
    debugError.textContent = 'Данные загружены из резервной копии. Проверьте файл contacts.json: ' + error.message;

    try {
      pageData = fallbackData;
      renderPage(pageData);
    } catch (fallbackError) {
      entranceList.innerHTML = '<p class="error">Не удалось построить список подъездов. Проверьте contacts.json и script.js.</p>';
      debugError.hidden = false;
      debugError.textContent = fallbackError.message;
    }
  }
}

modal.addEventListener('click', function (event) {
  if (event.target.hasAttribute('data-close-modal')) {
    closeModal();
  }
});

document.addEventListener('keydown', function (event) {
  if (event.key === 'Escape' && !modal.hidden) {
    closeModal();
  }
});

loadContacts();
